- name: Install ZSH
  become: true
  apt:
    name: zsh
    state: present
- name: Set ZSH as default shell
  user:
    name: "{{ current_user }}"
    shell: "{{ lookup('pipe', 'which zsh') }}"
  become: true
